This release renames the npm package to pi-mcporter and updates install instructions to use the unscoped package name.

## 🔧 Changes

### Rename npm package to pi-mcporter

Renamed the npm package from @mavam/pi-mcporter to pi-mcporter and updated install instructions to use the unscoped package name.

*By @mavam.*
