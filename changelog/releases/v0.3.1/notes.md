This release restores compatibility with pi 0.69 and later by updating TypeBox imports and Pi keybinding identifiers. Installed MCPorter extensions now load correctly after the pi runtime update.

## 🐞 Bug fixes

### Compatibility with pi 0.69

The extension now loads correctly with pi 0.69 and later.

Tool schemas and expand hints now use the current pi extension APIs, so installed MCPorter packages keep working after pi’s TypeBox and keybinding updates.

*By @mavam and @codex.*
