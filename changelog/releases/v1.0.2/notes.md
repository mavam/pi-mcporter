This release secures Pi MCPorter’s MCP transport dependency chain and removes the vulnerable Hono runtime from production installations.

## 🐞 Bug fixes

### Secure MCP dependency chain

Pi MCPorter no longer exposes users to known vulnerabilities in its MCP transport dependency chain. Upgrading removes the affected Hono runtime from production installations.

*By @mavam and @codex.*
