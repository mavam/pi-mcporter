{
	"name": "@mavam/pi-mcporter",
	"version": "0.1.1",
	"description": "Single-tool MCPorter bridge extension for pi and Model Context Protocol (MCP) servers.",
	"type": "module",
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"keywords": [
		"pi-package",
		"pi-extension",
		"coding-agent",
		"mcp",
		"model-context-protocol",
		"mcp-server",
		"mcp-tools",
		"mcp-proxy",
		"mcporter"
	],
	"author": "mavam",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/mavam/pi-mcporter.git"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"publishConfig": {
		"access": "public"
	},
	"pi": {
		"extensions": [
			"./dist/index.js"
		]
	},
	"scripts": {
		"build": "rm -rf dist && esbuild src/index.ts --bundle --format=esm --platform=node --outfile=dist/index.js --external:mcporter --external:@mariozechner/pi-coding-agent --external:@mariozechner/pi-ai --external:@sinclair/typebox",
		"prepare": "npm run build",
		"prepack": "npm run build",
		"check": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"mcporter": "^0.7.3"
	},
	"devDependencies": {
		"@mariozechner/pi-ai": "*",
		"@mariozechner/pi-coding-agent": "*",
		"@sinclair/typebox": "*",
		"@types/node": "^24.3.0",
		"esbuild": "^0.25.10",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18"
	}
}
